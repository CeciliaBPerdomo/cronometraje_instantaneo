para actualizar cambios de otra persona, con la terminal abierta
git pull origin master

para abrir area de trabajo
1. Terminal - new Terminal

levantar la pagina para verla en internet local
npm run dev
Entrar a  http://localhost:5173/ desde el chrome

Para enviar cambios a internet, en la terminal
ctrl + c
git add .
git commit -m "cambios"
git push origin master